apiVersion: v1
kind: Pod
metadata:
    name: nginx
    labels:
        app: nginx
        tier: frontend
spec:
    containers:
        - name: nginx
          image: nginx

#kubectl create -f pod.yml

#kubectl delete pod nginx
